services:
  - type: web
    name: lexia-backend
    env: node
    branch: main
    buildCommand: npm run build
    startCommand: npm start
    plan: free
    envVars:
      - key: SUPABASE_DATABASE_URL
        sync: false
      - key: PGSSLMODE
        value: require
