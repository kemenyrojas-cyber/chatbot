services:
  - type: web
    name: lexia-backend
    env: node
    branch: main
    buildCommand: npm run build
    startCommand: npm start
    plan: free
    envVars:
      - key: DATA_DIR
        value: /var/data
    disk:
      name: lexia-data
      mountPath: /var/data
      sizeGB: 1
